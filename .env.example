# Local development only — in CI/CD these are set as GitHub Variables
LAMBDA_ROLE_ARN=arn:aws:iam::<account-id>:role/service-role/<role-name>
SECRET_NAME=<your-secret-name>
AWS_PROFILE=<your-deployer-profile-name>
